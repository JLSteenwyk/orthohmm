# Complete Native Hierarchy Observation

Prospective integration test of the new complete-command hierarchy collector.
This is not a selective repeat to erase earlier interval flags or scientific
timing evidence. Preserve previous runs and evaluate all three methods.

Use the original645-protein,eight-species smoke specification SHA-256
`8af8a049c69b7e479e8443e1e0e14b74c78d67b345c03d7f557768488d057fa2`:
high-sensitivity OrthoHMM, satellite_v2 and full OrthoFinder3.1.5, once each
in that order, sequential exclusive spark-7ff0 allocations with20CPUs and
96GiB,900second native timeout. Relocate only output prefixes to a new
hierarchy_native_smoke_v1 directory. Preserve commands, inputs, environment,
runtime/system manifests and native output validation. No scientific parameter
is tuned and no existing output directory is overwritten.

The collector reads all immediate job step children between parent CPU reads,
themselves between host snapshots. Read the first observation before releasing
the native worker; check command completion before collecting the final point.
Keep the native step alive until the final memory counters are read. Store raw
read brackets/scopes/counters and complete command start/end/exit information.
Use one-second observations, preserving the existing0.5..1.5second gap gate.

The original interval screen is evaluated using the very same native-step
read and outer host brackets, with unchanged0.25average-core excess threshold,
negative0.5CPU-second discrepancy threshold and steal-time rule. Report all
flags. Additional parent/child diagnostics must not alter admission, hide
negative discrepancies, sum overlapping windows or correct wall time.

Require fresh recipe hashes, before/after runtime and system verification,
native partitions/pairs, exact replay of both old-threshold screens and new
hierarchy arithmetic, stable complete-command bounds and terminal scheduler
evidence. Any failed command, missing read or adverse flag remains reported.
Additional read cost, delayed accounting and non-CPU contention are not
calibrated by this integration. It cannot by itself authorize a new scientific
scaling panel or upgrade the original27timings.

Implementation status: measure_native_hierarchy_step.py and synthetic
complete-command tests are available. Frozen recipe assembly, native launcher,
actual three-method execution and independent output/counter audit remain
to be completed. No native hierarchy integration result is claimed yet.
